from setuptools import setup, find_packages

setup(
    name='gptools',
    version='0.1',  # package version
    description='',
    license='MIT',
    packages=find_packages(),
    install_requires=[
    ],
    zip_safe=True
)
